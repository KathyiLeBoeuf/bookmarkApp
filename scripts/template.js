/* global $ */
'use strict';
/*
template for stars
<li name="stars" class="form__stars js-book__details--li">
					<span>☆</span>
					<span>☆</span>
					<span>☆</span>
					<span>☆</span>
					<span>☆</span>
					<span>☆</span>
				</li>
				<li name="stars" class="form__stars js-book__details--li">
					<span>&#9733</span>
					<span>&#9733</span>
					<span>&#9733</span>
					<span>&#9734</span>
					<span>&#9734</span>
					<span>&#9734</span>

				</li>
				*/




function renderBookmarksList() {
  // this function will be responsible for rendering bookmarks in
	// the DOM
	//get request talk to api

  console.log('`renderBookmarkList` ran');
}


function addNewBookmark() {
  // this function will be responsible for when users add a new shopping list item
  console.log('`addNewBookmark` ran');
}
const validate (url, title) {}
//POST request

function deleteBookmark() {
  // this function will be responsible for when users delete
  console.log('`deleteBookmark` ran');
}

function expandBookmark() {
  // this function will be responsible for when users click the "check" button on
  // a shopping list item.
  console.log('`expandBookmark` ran');
}

function filterBookmarks() {
  // this function will be responsible for when users click the "check" button on
  // a shopping list item.
  console.log('`filterBookmarks` ran');
}




// this function will be our callback when the page loads. it's responsible for
// initially rendering the shopping list, and activating our individual functions
// that handle new item submission and user clicks on the "check" and "delete" buttons
// for individual shopping list items.
return {


};

// when the page loads, call `handleShoppingList`
$();